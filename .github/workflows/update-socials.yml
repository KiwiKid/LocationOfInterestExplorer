name: "Update socials"

on:
  schedule:
    - cron: '*/30 * * * *'
jobs:
  deployment:
    runs-on: ubuntu-latest
    steps:
    - name: Trigger Vercel Build
      uses: fjogeleit/http-request-action@master
      with:
        url: ${{ secrets.SOCIALS_UPDATE_URL }}
        method: 'GET'